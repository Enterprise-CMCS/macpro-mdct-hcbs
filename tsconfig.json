{
  "compilerOptions": {
    "target": "ES2020",
    "module": "es2020",
    "moduleResolution": "node",
    "lib": ["ES2020"],
    "strict": true,
    "esModuleInterop": true,
    /* The only special thing in this config is where we put the built files. */
    "outDir": "build_dev",
  },
  "exclude": [
    "./services/*",
    "./tests/*"
  ]
}
